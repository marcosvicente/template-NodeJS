// import request from 'supertest'
//import app  from '../src/app'

//describe('GET /api', () => {
//  it('should be reponse with json', async () => {
//        const response = await request(app).get('/api');
//        expect(response.status).toBe(201);
//    });
//})
